export interface AliasToRelativePathOptions {
    filePath: string;
    imports: string[];
    config: {
        baseUrl?: string;
        paths?: {
            [key: string]: [];
        };
    };
    cwd?: string;
}
export declare function aliasToRelativePath({ filePath, imports, config, cwd, }: AliasToRelativePathOptions): string[];
