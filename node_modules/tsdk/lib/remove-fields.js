"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeFields = void 0;
const glob = require("fast-glob");
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
const config_1 = require("./config");
function removeFields() {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        if (((_a = config_1.config.removeFields) === null || _a === void 0 ? void 0 : _a.length) === 0)
            return;
        const pattern = path_1.default.join(config_1.ensureDir, `lib/**/*.${config_1.config.apiconfExt}.js`).replace(/\\/g, '/');
        const removeFields = config_1.config.removeFields || [
            'needAuth',
            'category',
            'name',
            'description',
            'type',
        ];
        const files = yield glob(pattern);
        yield Promise.all(files.map((file) => __awaiter(this, void 0, void 0, function* () {
            let content = yield fs_extra_1.default.readFile(file, 'utf8');
            removeFields.forEach((field) => {
                content = content.replace(new RegExp(`${field}:.*\n`), '');
                // content = content.replace(new RegExp(`${field}:.*`), '');
            });
            return fs_extra_1.default.writeFile(file, content);
        })));
    });
}
exports.removeFields = removeFields;
