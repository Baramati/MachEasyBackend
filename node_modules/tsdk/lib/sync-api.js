"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.copyPermissionsJSON = exports.syncAPI = void 0;
const fast_glob_1 = __importDefault(require("fast-glob"));
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
const config_1 = require("./config");
const format_1 = require("./format");
const symbols_1 = __importDefault(require("./symbols"));
const baseDir = path_1.default.join(path_1.default.relative(path_1.default.dirname(__filename), process.cwd()), config_1.ensureDir);
function syncAPI() {
    return __awaiter(this, void 0, void 0, function* () {
        console.log(symbols_1.default.bullet, 'generating APIs');
        const pkgJSON = require(path_1.default.join(baseDir, 'package.json'));
        const apiconfs = require(path_1.default.join(baseDir, 'lib', `${config_1.config.apiconfExt}-refs`));
        const keys = Object.keys(apiconfs);
        keys.sort();
        const types = [...new Set(keys.map((k) => apiconfs[k].type))].filter((i) => !!i);
        if (!types.includes('common')) {
            types.push('common');
        }
        types.sort();
        types.forEach((apiType) => {
            const headStr = `
      /** 
       * 
       * api-${apiType}.ts 
       * ${config_1.config.packageName}@${pkgJSON.version} 
       * 
       **/

      import genApi from './gen-api';
`;
            let importStr = ``;
            let bodyStr = ``;
            const hasCommon = keys.find((k) => {
                const item = apiconfs[k];
                return (item.type === 'common' || !item.type) && item.name;
            });
            const exportStr = apiType === 'common' || !hasCommon ? `` : `\nexport * from './common-api';\n`;
            let hasContentCount = 0;
            keys.forEach((k, idx) => {
                const { name, description, type: _type, category = 'others' } = apiconfs[k];
                const type = _type === 'common' || !_type ? 'common' : _type;
                if (type === apiType && name) {
                    importStr += `
          ${name}Config,
          ${name}Req,
          ${name}Res,
        `;
                    bodyStr += `
          /** 
           * ${description}
           * 
           * @category ${category}
           */
          export const ${name} = genApi<${name}Req, ${name}Res>(${name}Config);
        `;
                    hasContentCount++;
                }
            });
            if (hasContentCount > 0) {
                const content = `
      ${headStr}
      ${importStr
                    ? `import {
          ${importStr}
        } from './${config_1.config.apiconfExt}-refs';`
                    : ''}
      ${exportStr}
      ${bodyStr}
    `;
                fs_extra_1.default.writeFileSync(path_1.default.join(config_1.ensureDir, `src`, `${apiType}-api.ts`), (0, format_1.formatTS)(content));
            }
        });
        console.log(symbols_1.default.success, 'generated APIs');
        const exportPermissions = {};
        keys.forEach((k) => {
            const item = apiconfs[k];
            if (!exportPermissions[item.type]) {
                exportPermissions[item.type] = [];
            }
            if (item.schema) {
                item.schema = {};
            }
            exportPermissions[item.type].push(item);
        });
        yield fs_extra_1.default.writeFile(path_1.default.join(config_1.ensureDir, 'src', `permissions.json`), JSON.stringify(exportPermissions, null, 2));
        console.log(symbols_1.default.bullet, 'Docs config');
        // sync APIs docs
        const links = [];
        types.forEach((apiType) => {
            if (apiType === 'common')
                return;
            links.push(`- [${apiType} APIs](/docs/api/modules/${apiType}_api)`);
        });
        const files = yield (0, fast_glob_1.default)(config_1.config.sharedDirs.map((i) => path_1.default.join(i, '**', `*.ts`).replace(/\\/g, '/')));
        files.forEach((i) => {
            const arr = i.split('/');
            arr.shift();
            links.push(`- [${arr.join('/').replace('.ts', '')}](${'/docs/api/modules/' + arr.join('_').replace('.ts', '').replace(/-/g, '_')})`);
        });
        links.push('- [all reference](/docs/api/modules)');
        const projectName = `%PROJECT NAME%`;
        try {
            let getStartedContent = yield fs_extra_1.default.readFile(path_1.default.join(__dirname, '..', 'fe-sdk-template', 'website', 'docs', 'get-started.md'), 'utf-8');
            getStartedContent = getStartedContent
                .replace(new RegExp(projectName, 'g'), config_1.config.packageName)
                .replace('%API_REFERENCE%', links.join('\n'));
            yield fs_extra_1.default.writeFile(path_1.default.join(config_1.ensureDir, 'website', 'docs', 'get-started.md'), getStartedContent);
            let docusaurusConfigContent = yield fs_extra_1.default.readFile(path_1.default.join(config_1.ensureDir, 'website', 'docusaurus.config.js'), 'utf-8');
            docusaurusConfigContent = docusaurusConfigContent.replace(new RegExp(projectName, 'g'), config_1.config.packageName);
            yield fs_extra_1.default.writeFile(path_1.default.join(config_1.ensureDir, 'website', 'docusaurus.config.js'), docusaurusConfigContent);
            console.log(symbols_1.default.success, 'Docs config');
        }
        catch (e) {
            if (e instanceof Error) {
                console.log(symbols_1.default.error, 'Docs config error', e.message);
            }
        }
    });
}
exports.syncAPI = syncAPI;
function copyPermissionsJSON() {
    const dist = path_1.default.join(config_1.ensureDir, `lib`, `permissions.json`);
    console.log(symbols_1.default.info, `copy \`permission.json\` to \`${dist}\``);
    return fs_extra_1.default.copy(path_1.default.join(config_1.ensureDir, `src`, `permissions.json`), dist, {
        overwrite: true,
    });
}
exports.copyPermissionsJSON = copyPermissionsJSON;
