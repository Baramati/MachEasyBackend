"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteFilesBeforeSync = void 0;
const glob = require("fast-glob");
const fs_extra_1 = __importDefault(require("fs-extra"));
const path_1 = __importDefault(require("path"));
const config_1 = require("./config");
const symbols_1 = __importDefault(require("./symbols"));
function deleteFilesBeforeSync() {
    return __awaiter(this, void 0, void 0, function* () {
        const preWhiteList = yield glob(path_1.default.join(__dirname, '../fe-sdk-template/src/**').replace(/\\/g, '/'));
        const whiteList = [
            ...preWhiteList.map((i) => i.replace(path_1.default.join(__dirname, '../fe-sdk-template'), config_1.ensureDir)),
            ...config_1.config.sdkWhiteList.map((i) => {
                return path_1.default.join(config_1.ensureDir, path_1.default.normalize(i).replace(config_1.ensureDir, ''));
            }),
        ];
        const whiteFiles = yield glob(whiteList.map((i) => {
            return i.replace(/\\/g, '/');
        }));
        let files = yield glob([path_1.default.join(config_1.ensureDir, 'src/**').replace(/\\/g, '/')], {
            ignore: whiteFiles,
        });
        // special for windows
        files = files.filter((i) => whiteFiles.find((j) => i === j));
        yield Promise.all([files.map((i) => fs_extra_1.default.remove(i))]);
        console.log(symbols_1.default.success, 'deleted cache files');
    });
}
exports.deleteFilesBeforeSync = deleteFilesBeforeSync;
